#include <stdio.h>
#include <stdlib.h>

int main()
{
    Hash* H;

    Hash* ha = criaHash(1427);

    int i, N;
    scanf("%d\f",&N);
    for(i=0;i< N ;i++){
        insereHash_EnderAberto(ha,in);
    }
    return 0;
}
